@model IEnumerable<FacturadorApi.Model.Factura_Detalle>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FechaAlta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cant)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Precio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Monto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fact_ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Art_ID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaAlta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cant)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Precio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Monto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fact_ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Art_ID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FC_DTL_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FC_DTL_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FC_DTL_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
